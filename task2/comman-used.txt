- find block -name "*.c" | wc -l

- find block -name "*.c" -exec cat {} + | wc -l

- find block -name "*.c" -exec wc -l {} + | sort -nr | head -4

- grep -r "^config" block/Kconfig | wc -l

- grep -rn "struct request {"

- grep -rn "struct bio {"

- grep -r "add_disk"

- grep -rn "void device_add_disk"

- grep -rn "device_add_disk"

- grep "EXPORT_SYMBOL" block/*.c | head -5 

- grep -h "^#include <linux/" block/*.c | sort | uniq -c | sort -nr | head -5
